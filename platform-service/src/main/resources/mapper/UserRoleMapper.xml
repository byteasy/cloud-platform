<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.byteasy.platform.mapper.UserRoleMapper">

    <select id="getUserRoleIdsByUserId" resultType="java.lang.Long">
        SELECT DISTINCT ro.id
        FROM platform_user_role ur
                 INNER JOIN platform_role ro ON ur.role_id = ro.id
        WHERE ur.user_id = #{userId}
          and ro.status = 1
    </select>
</mapper>
